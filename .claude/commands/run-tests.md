Run all three test suites for this project and report a summary of pass/fail counts.

```
python tests/test_calc.py
python tests/test_synth.py
python tests/test_alignment.py
```

Run each one, capture the output, and present a clean summary table: test file, passed, failed, any error messages. If any tests fail, show the failure details and suggest likely causes based on the CLAUDE.md architecture notes.
